// Define the actor's full name
const fullName = "Leonardo DiCaprio"; // Replace with your favorite actor's full name

// Create the message with the full name in uppercase
const message = `My favorite actor is ${fullName.toUpperCase()}`;

// Log the message to the console
console.log(message);
